diff --git a/src/app/[locale]/deckmarket/[deckId]/page.tsx b/src/app/[locale]/deckmarket/[deckId]/page.tsx
index f31d5cb1..a1c42aa3 100644
--- a/src/app/[locale]/deckmarket/[deckId]/page.tsx
+++ b/src/app/[locale]/deckmarket/[deckId]/page.tsx
@@ -7,9 +7,12 @@ import MobileNavSpacer from '@/components/layout/MobileNavSpacer'
 import StructuredData from '@/components/StructuredData'
 import { useI18n } from '@/i18n/I18nContext'
 import { useAuth } from '@/hooks/useAuth'
+import { useSubscription } from '@/hooks/useSubscription'
 import type { DeckMarketDeck, DeckMarketVersion } from '@/types/deckmarket'
 import { cn } from '@/lib/utils'
 import { useParams } from 'next/navigation'
+import Link from 'next/link'
+import { Plus, CheckCircle2, ArrowRight, Loader2 } from 'lucide-react'
 
 function formatBytes(bytes: number): string {
   if (!bytes) return '0 B'
@@ -38,6 +41,12 @@ export default function DeckMarketDetailPage() {
   const [downloadingId, setDownloadingId] = useState<string | null>(null)
   const [downloadError, setDownloadError] = useState<string | null>(null)
 
+  // "Add to Flashcards" state
+  const { isPremium } = useSubscription()
+  type AddState = 'idle' | 'downloading' | 'parsing' | 'saving' | 'success' | 'already-added'
+  const [addState, setAddState] = useState<AddState>('idle')
+  const [addError, setAddError] = useState<string | null>(null)
+
   useEffect(() => {
     if (!deckId) return
     const loadDeck = async () => {
@@ -59,6 +68,83 @@ export default function DeckMarketDetailPage() {
     void loadDeck()
   }, [deckId])
 
+  // Detect "already added" state by matching deck name + deckmarket origin
+  useEffect(() => {
+    if (!deckId || !user || !deck) return
+    let cancelled = false
+    const check = async () => {
+      try {
+        const { ankiDeckManager } = await import('@/lib/anki/AnkiDeckManager')
+        const decks = await ankiDeckManager.getDecks(user.uid, isPremium ?? false)
+        if (cancelled) return
+        const found = decks.some(d => d.origin === 'deckmarket' && d.name === deck.title)
+        if (found) setAddState('already-added')
+      } catch {
+        // ignore — detection is best-effort
+      }
+    }
+    void check()
+    return () => { cancelled = true }
+  }, [deckId, user, deck, isPremium])
+
+  // "Add to Flashcards" handler
+  const handleAddToFlashcards = async () => {
+    if (!deckId || !user || !deck) return
+    setAddState('downloading')
+    setAddError(null)
+
+    try {
+      // 1. Get presigned download URL
+      const dlRes = await fetch(`/api/deckmarket/decks/${deckId}/download?format=apkg`)
+      if (!dlRes.ok) throw new Error('Download failed')
+      const dlData = await dlRes.json()
+
+      // 2. Download the .apkg blob
+      const blobRes = await fetch(dlData.downloadUrl)
+      if (!blobRes.ok) throw new Error('Download failed')
+      const blob = await blobRes.blob()
+      const filename = dlData.filename || `${deck.title}.apkg`
+      const file = new File([blob], filename, { type: 'application/zip' })
+
+      // 3. Parse the .apkg
+      setAddState('parsing')
+      const { AnkiImporter } = await import('@/lib/anki/importer')
+      const parsed = await AnkiImporter.parsePackage(file)
+
+      const parsedDeck = parsed.decks.find(d => d.cards.length > 0) || parsed.decks[0]
+      if (!parsedDeck) throw new Error('No cards found')
+
+      // 4. Store media
+      setAddState('saving')
+      const { AnkiMediaStore, buildAnkiMediaKey } = await import('@/lib/anki/mediaStore')
+      const mediaStore = AnkiMediaStore.getInstance()
+
+      for (const [mediaFilename, mediaBlob] of parsed.media) {
+        const key = buildAnkiMediaKey(parsedDeck.id, mediaFilename)
+        await mediaStore.storeMedia(key, mediaBlob)
+      }
+
+      // 5. Override deck name with DeckMarket title for user-friendliness
+      parsedDeck.name = deck.title
+      parsedDeck.origin = 'deckmarket'
+
+      // 6. Save deck — premium users get R2 backup
+      const { ankiDeckManager } = await import('@/lib/anki/AnkiDeckManager')
+      await ankiDeckManager.saveDeck(parsedDeck, user.uid, isPremium ?? false, filename, isPremium ?? false)
+
+      setAddState('success')
+    } catch (err: any) {
+      if (err?.code === 'DUPLICATE_DECK_NAME') {
+        setAddError(strings.deckmarket.deck.addDuplicateName)
+      } else if (err?.code === 'DECKMARKET_LIMIT_REACHED') {
+        setAddError(strings.deckmarket.deck.addLimitReached)
+      } else {
+        setAddError(strings.deckmarket.deck.addFailed)
+      }
+      setAddState('idle')
+    }
+  }
+
   const handleDownload = async (versionId?: string, format: 'apkg' | 'csv' = 'apkg') => {
     if (!deckId) return
     const downloadKey = `${versionId || 'latest'}-${format}`
@@ -243,8 +329,59 @@ export default function DeckMarketDetailPage() {
                         : strings.deckmarket.deck.downloadCsv}
                     </button>
                   )}
+
+                  {/* Add to Flashcards button */}
+                  {latestVersion && (
+                    <button
+                      onClick={handleAddToFlashcards}
+                      disabled={addState !== 'idle'}
+                      aria-label={strings.deckmarket.deck.addToFlashcards}
+                      className={cn(
+                        'inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium transition-colors',
+                        addState === 'idle'
+                          ? 'bg-primary-500 hover:bg-primary-600 text-white'
+                          : addState === 'success' || addState === 'already-added'
+                            ? 'bg-green-500 text-white cursor-default'
+                            : 'bg-gray-400 text-white cursor-not-allowed'
+                      )}
+                    >
+                      {addState === 'idle' && <Plus className="h-4 w-4" />}
+                      {(addState === 'downloading' || addState === 'parsing' || addState === 'saving') && (
+                        <Loader2 className="h-4 w-4 animate-spin" />
+                      )}
+                      {(addState === 'success' || addState === 'already-added') && (
+                        <CheckCircle2 className="h-4 w-4" />
+                      )}
+                      {addState === 'idle' && strings.deckmarket.deck.addToFlashcards}
+                      {(addState === 'downloading' || addState === 'parsing' || addState === 'saving') &&
+                        strings.deckmarket.deck.adding}
+                      {addState === 'success' && strings.deckmarket.deck.added}
+                      {addState === 'already-added' && strings.deckmarket.deck.alreadyAdded}
+                    </button>
+                  )}
                 </div>
               </div>
+
+              {/* Add-to-flashcards error display */}
+              {addError && (
+                <div className="bg-red-100 dark:bg-red-900/30 text-red-800 dark:text-red-200 p-3 rounded-lg text-sm mt-4">
+                  {addError}
+                </div>
+              )}
+
+              {/* "Go to Flashcards" link after success */}
+              {(addState === 'success' || addState === 'already-added') && (
+                <div className="mt-4">
+                  <Link
+                    href={`/${locale}/flashcards`}
+                    className="inline-flex items-center gap-1.5 text-sm font-medium text-primary-600 dark:text-primary-400 hover:underline"
+                  >
+                    {strings.deckmarket.deck.goToFlashcards}
+                    <ArrowRight className="h-3.5 w-3.5" />
+                  </Link>
+                </div>
+              )}
+
               {!latestVersion && (
                 <p className="text-sm text-gray-500 dark:text-gray-400 mt-3">
                   {strings.deckmarket.admin.noVersions}
diff --git a/src/i18n/locales/de/strings.ts b/src/i18n/locales/de/strings.ts
index 36ff49ed..134fb151 100644
--- a/src/i18n/locales/de/strings.ts
+++ b/src/i18n/locales/de/strings.ts
@@ -7734,6 +7734,14 @@ export const strings = {
       notFound: 'Deck nicht gefunden',
       changelog: 'Änderungsprotokoll',
       loginRequired: 'Bitte melde dich an, um Decks zu durchsuchen',
+      addToFlashcards: 'Zu Karteikarten hinzufügen',
+      adding: 'Wird hinzugefügt...',
+      added: 'Zu Karteikarten hinzugefügt',
+      alreadyAdded: 'Bereits in deinen Karteikarten',
+      goToFlashcards: 'Zu Karteikarten',
+      addFailed: 'Deck konnte nicht hinzugefügt werden. Bitte versuche es erneut.',
+      addDuplicateName: 'Ein Deck mit diesem Namen existiert bereits in deinen Karteikarten.',
+      addLimitReached: 'Kostenlose Nutzer können ein DeckMarket-Deck haben. Entferne dein aktuelles Deck oder upgrade auf Premium.',
     },
     pagination: {
       prev: 'Zurück',
diff --git a/src/i18n/locales/en/strings.ts b/src/i18n/locales/en/strings.ts
index 9b540ea0..5d0c857b 100644
--- a/src/i18n/locales/en/strings.ts
+++ b/src/i18n/locales/en/strings.ts
@@ -7966,6 +7966,14 @@ export const strings = {
       notFound: 'Deck not found',
       changelog: 'Changelog',
       loginRequired: 'Please log in to browse decks',
+      addToFlashcards: 'Add to Flashcards',
+      adding: 'Adding...',
+      added: 'Added to Flashcards',
+      alreadyAdded: 'Already in your Flashcards',
+      goToFlashcards: 'Go to Flashcards',
+      addFailed: 'Failed to add deck. Please try again.',
+      addDuplicateName: 'A deck with this name already exists in your Flashcards.',
+      addLimitReached: 'Free users can have one DeckMarket deck. Remove your current deck first, or upgrade to Premium.',
     },
     pagination: {
       prev: 'Prev',
diff --git a/src/i18n/locales/es/strings.ts b/src/i18n/locales/es/strings.ts
index 99981652..b0ea35bf 100644
--- a/src/i18n/locales/es/strings.ts
+++ b/src/i18n/locales/es/strings.ts
@@ -7728,6 +7728,14 @@ export const strings = {
       notFound: 'Deck no encontrado',
       changelog: 'Registro de cambios',
       loginRequired: 'Inicia sesión para explorar decks',
+      addToFlashcards: 'Agregar a Tarjetas',
+      adding: 'Agregando...',
+      added: 'Agregado a Tarjetas',
+      alreadyAdded: 'Ya está en tus Tarjetas',
+      goToFlashcards: 'Ir a Tarjetas',
+      addFailed: 'Error al agregar el deck. Inténtalo de nuevo.',
+      addDuplicateName: 'Ya existe un deck con este nombre en tus Tarjetas.',
+      addLimitReached: 'Los usuarios gratuitos pueden tener un deck de DeckMarket. Elimina tu deck actual o actualiza a Premium.',
     },
     pagination: {
       prev: 'Anterior',
diff --git a/src/i18n/locales/fr/strings.ts b/src/i18n/locales/fr/strings.ts
index ddeebeee..32ab9637 100644
--- a/src/i18n/locales/fr/strings.ts
+++ b/src/i18n/locales/fr/strings.ts
@@ -7539,6 +7539,14 @@ export const strings = {
       notFound: 'Deck introuvable',
       changelog: 'Journal des modifications',
       loginRequired: 'Veuillez vous connecter pour parcourir les decks',
+      addToFlashcards: 'Ajouter aux Flashcards',
+      adding: 'Ajout en cours...',
+      added: 'Ajouté aux Flashcards',
+      alreadyAdded: 'Déjà dans vos Flashcards',
+      goToFlashcards: 'Aller aux Flashcards',
+      addFailed: "Échec de l'ajout du deck. Veuillez réessayer.",
+      addDuplicateName: 'Un deck portant ce nom existe déjà dans vos Flashcards.',
+      addLimitReached: 'Les utilisateurs gratuits peuvent avoir un deck DeckMarket. Supprimez votre deck actuel ou passez à Premium.',
     },
     pagination: {
       prev: 'Précédent',
diff --git a/src/i18n/locales/it/strings.ts b/src/i18n/locales/it/strings.ts
index 0f0987c9..f8a26237 100644
--- a/src/i18n/locales/it/strings.ts
+++ b/src/i18n/locales/it/strings.ts
@@ -7763,6 +7763,14 @@ export const strings = {
       notFound: 'Deck non trovato',
       changelog: 'Registro modifiche',
       loginRequired: 'Accedi per sfogliare i deck',
+      addToFlashcards: 'Aggiungi alle Flashcard',
+      adding: 'Aggiunta in corso...',
+      added: 'Aggiunto alle Flashcard',
+      alreadyAdded: 'Già nelle tue Flashcard',
+      goToFlashcards: 'Vai alle Flashcard',
+      addFailed: 'Impossibile aggiungere il deck. Riprova.',
+      addDuplicateName: 'Un deck con questo nome esiste già nelle tue Flashcard.',
+      addLimitReached: 'Gli utenti gratuiti possono avere un deck DeckMarket. Rimuovi il tuo deck attuale o passa a Premium.',
     },
     pagination: {
       prev: 'Precedente',
diff --git a/src/i18n/locales/ja/strings.ts b/src/i18n/locales/ja/strings.ts
index 7e617a73..92050ae3 100644
--- a/src/i18n/locales/ja/strings.ts
+++ b/src/i18n/locales/ja/strings.ts
@@ -7697,6 +7697,14 @@ export const strings = {
       notFound: 'デッキが見つかりません',
       changelog: '変更履歴',
       loginRequired: 'デッキを閲覧するにはログインしてください',
+      addToFlashcards: 'フラッシュカードに追加',
+      adding: '追加中...',
+      added: 'フラッシュカードに追加しました',
+      alreadyAdded: 'すでにフラッシュカードに追加済みです',
+      goToFlashcards: 'フラッシュカードへ',
+      addFailed: 'デッキの追加に失敗しました。もう一度お試しください。',
+      addDuplicateName: '同じ名前のデッキがすでにフラッシュカードに存在します。',
+      addLimitReached: '無料ユーザーはDeckMarketのデッキを1つまで追加できます。現在のデッキを削除するか、プレミアムにアップグレードしてください。',
     },
     pagination: {
       prev: '前へ',
